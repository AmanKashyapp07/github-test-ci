{
  "name": "github-test-ci",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage",
    "lint": "node lint.js",
    "build": "node build.js"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
